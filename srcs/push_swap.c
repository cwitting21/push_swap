#include "../includes/push_swap.h"

int				main(int ac, char **av)
{
	// t_stack		stack;

	if (ac > 1)
	{
		// stack.a = NULL;
		// stack.b = NULL;
		//initialise_stack(&stack);
		args_to_lst(ac, av/*, &stack*/);
	}
	// printf("%d\n", stack.a->value);
	// printf("%d\n", stack.a->next->value);
	return (0);
}
